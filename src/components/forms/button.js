import React from "react";
import { Text, StyleSheet } from "react-native";

const Button = () => {
    return <Text style={styles.title}>Sanguine</Text>
}

const styles = StyleSheet.create({
    title: {
        fontSize: 60,
    },
});

export default Button;